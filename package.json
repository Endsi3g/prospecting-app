{
  "name": "prospect-app",
  "version": "1.0.0",
  "description": "Application de prospection automatisée avec intégration LLM",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "apify-client": "^2.21.0",
    "cors": "^2.8.5",
    "csv-parse": "^5.5.2",
    "exceljs": "^4.4.0",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "playwright": "^1.57.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}